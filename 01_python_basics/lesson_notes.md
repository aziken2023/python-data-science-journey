- When using Git and trying to push to Github you first check "git main", then "git add .", then you commit to git with "git commit -m "and the message"", then after you push to github with "git push".
- When converting from one datatype to another, you put the variable then the datatype and the variable in a bracket for example 
age = int(age).